---
dependencies:
  - name: nginx-proxy
    NGINX_PROXY_CONFIG_PATH: "{{ MATOMO_NGINX_CONFIG_PATH }}"
    NGINX_PROXY_PUBLIC_PORT: 443
    NGINX_PROXY_INTERNAL_PORT: 8080
    NGINX_PROXY_USE_SSL: True
    NGINX_PROXY_SERVER_NAME: "{{ MATOMO_APP_DOMAIN }}"
