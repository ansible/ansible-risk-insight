Ansible role for deploying WordPress using docker
=================================================

This role deploys a WordPress instance using `docker-compose` amd sets up an nginx reverse proxy in front of it.

## Variables
See `defaults/main.yml`.
